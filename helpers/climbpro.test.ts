import { describe, it, expect } from "vitest";
import { climbpro } from "./climbpro";

describe("climbpro test suite", () => {
  it("should do some stuff", () => {
    // arrange
    const data = [1, 2, 3, 4, 5, 4, 5, 6, 7, 8, 9];

    // act
    // climbpro(data);

    // assert
    expect(true).toBe(true);
  });
});
